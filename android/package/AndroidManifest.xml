<?xml version="1.0"?>
<!--
    Имя пакета org.makgrib.app задано здесь и после первой публикации в
    магазине не меняется никогда. Прежнее, org.qtproject.example.MAKGrib,
    было заготовкой Qt: со словом example внутри приложение так бы и
    осталось навсегда.

    Свой манифест нужен ради одной строки: android:screenOrientation="portrait".
    Экран заблокирован по вертикали намеренно — лёжа телефон в рубке держать
    неудобно, а карта и панель рассчитаны на высокий экран.

    Разрешения на внешнюю память нет: карты из APK и скачанный прогноз лежат
    во внутренней папке приложения, а это разрешение в Google Play требует
    отдельных объяснений.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.makgrib.app"
    android:installLocation="auto"
    android:versionCode="120"
    android:versionName="1.2.0">
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"  />
    <uses-permission android:name="android.permission.INTERNET"  />
    <!-- Своё место на карте. Грубое разрешение идёт следом за точным:
         без него Android не отдаёт место вовсе, если точное отклонили. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true" />
    <application
        android:name="org.qtproject.qt.android.bindings.QtApplication"
        android:hardwareAccelerated="true"
        android:label="MAKGrib"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="true"
        android:fullBackupOnly="false">
        <activity
            android:name="org.qtproject.qt.android.bindings.QtActivity"
            android:configChanges="orientation|uiMode|screenLayout|screenSize|smallestScreenSize|layoutDirection|locale|fontScale|keyboard|keyboardHidden|navigation|mcc|mnc|density"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

          <meta-data
                android:name="android.app.lib_name"
                android:value="MAKGrib" />

          <meta-data
                android:name="android.app.arguments"
                android:value="" />
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.qtprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/qtprovider_paths"/>
        </provider>
    </application>
</manifest>
